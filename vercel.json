{
     "version": 2,
     "builds": [
       { "src": "package.json", "use": "@vercel/static-build" }
     ],
     "routes": [
       { "handle": "filesystem", "src": "/(.*\\.(json|xml|txt|css|js))", "method": "GET" },
       { "src": "/(.*)", "dest": "/public/$1" }
     ]
   }